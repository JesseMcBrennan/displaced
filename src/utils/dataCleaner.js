import { membersFetch } from './fetchCalls.js'

export const membersCleaner = async () => {
  // const fetchedMemberData = await membersFetch();
  // const memberUrl = await fetchedMemberData.results[0].members.map(member => member.api_uri)
  // debugger;
  // return memberUrl
}